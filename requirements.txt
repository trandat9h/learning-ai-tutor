# AI Tutor — Python dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE: the 'graphviz' pip package is a Python wrapper only.
# The underlying Graphviz binaries must also be installed at the OS level:
#   macOS  →  brew install graphviz
#   Ubuntu →  sudo apt-get install graphviz
#   Windows→  https://graphviz.org/download/

# ── Core AI ───────────────────────────────────────────────────────────────────
openai>=1.30.0

# ── Notebook runtime ─────────────────────────────────────────────────────────
jupyterlab>=4.0.0        # run with: jupyter lab src.ipynb
ipywidgets>=8.0.0        # button-based navigation UI
widgetsnbextension>=4.0  # enables ipywidgets in JupyterLab/Notebook

# ── Visualisation ─────────────────────────────────────────────────────────────
graphviz>=0.20.0         # Python wrapper (requires OS-level graphviz binary — see note above)
textual